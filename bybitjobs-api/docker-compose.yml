version: '3.8'

services:
  bybitjobs-api:
    build: .
    container_name: bybitjobs-api
    restart: always
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - NODE_ENV=production
      - EMAIL_USER=thanhpnvpbq@gmail.com
      - EMAIL_PASS=uuto vgwt hcms rnif
